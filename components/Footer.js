import Head from "next/head";

export default function Footer({ children }) {
  return (
    <>
      <footer className="w-full h-24 p-2 flex items-center justify-between text-white bg-darkBackground">
        <p>Todos os bla bla</p>
      </footer>
    </>
  );
}
